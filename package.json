{
  "name": "atlas",
  "private": true,
  "engines": {
    "node": ">=21",
    "pnpm": ">=9"
  },
  "scripts": {
    "format": "prettier --write .",
    "app:lint:fix": "pnpm --filter op-atlas lint:fix"
  }
}
